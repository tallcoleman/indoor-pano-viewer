[project]
name = "indoor-pano-viewer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ben Coleman", email = "ben.coleman@alum.utoronto.ca" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
indoor-pano-viewer = "indoor_pano_viewer:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
